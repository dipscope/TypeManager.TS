import { CustomContext } from './custom-context';

/**
 * Type internals.
 * 
 * @type {TypeInternals}
 */
export type TypeInternals =
{
    /**
     * Custom context.
     * 
     * @type {CustomContext}
     */
    customContext?: CustomContext;
}
